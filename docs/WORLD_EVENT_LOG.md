# 文明编年史事件日志

`WorldEventLog` 保存最近 40 条文明事件，超过容量时删除最早记录。日志记录时代跃迁、科技突破、外交行动、天气灾害、火箭发射、空间站建设、深空探测、载人探索以及国家 AI 自动行动。

HUD 状态提供 `LatestWorldEvent` 和 `WorldEventCount`，可用于顶部滚动提示、事件面板或编年史页面。完整事件数组会写入 JSON 存档，加载后恢复最近事件。

事件日志只记录已经完成的领域结果，不作为模拟数据源。这样即使 UI 被关闭或特效播放失败，玩家仍可从编年史查看文明发生过的关键事件。
